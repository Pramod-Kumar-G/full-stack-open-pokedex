name: Code Linting

on:
  push:
    branches:
      - main

jobs:
  run_lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - name: install dependencies
        run: npm install
      - name: check style
        run: npm run eslint
